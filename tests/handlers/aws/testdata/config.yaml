inputs:
  - type: "cloudwatch-logs"
    id: "$CloudwatchLogStreamARN"
    tags:
        - test_tag
    outputs:
      - type: "logstash"
        args:
          logstash_url: "$LogstashURL"
          # username: "{self._secret_arn}:username"
          # password: "{self._secret_arn}:password"
